import { makeStyles } from "@material-ui/core/styles"

export default makeStyles(() => ({
  fileInput: {
    border: "1px dashed black",
    width: "fit-content",
    padding: "4px",
    margin: "4px",
    height: "100px",
  },
}))
