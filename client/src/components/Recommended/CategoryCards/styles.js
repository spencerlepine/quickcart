import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  gridView: {
    display: "flex",
    overflow: "overlay",
    margin: "0 auto 5% auto",
    backgroundColor: "#fff",
  },
}));
