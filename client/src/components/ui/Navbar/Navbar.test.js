// import React from 'react';
// import { screen, render } from 'test-utils';
// import Navbar from './Navbar';

describe('Navbar', () => {
  test('renders the hamburger menu button on left-handside', () => {
    expect(true).toBe(true);
  });

  test('renders logo with link to HOME in the middle', () => {
    expect(true).toBe(true);
  });

  test('renders the cart button on right-hand side', () => {
    expect(true).toBe(true);
  });
});
