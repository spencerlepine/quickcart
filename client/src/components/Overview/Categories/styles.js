import { makeStyles } from "@material-ui/core/styles"

export default makeStyles(() => ({
  categoriesContainter: {
    paddingTop: "20px",
    margin: "auto",
    marginTop: "15px",
    width: "fit-content",
    minWidth: "400px",
    minHeight: "60px",
    height: "fit-content",
    maxWidth: "80vw",
    overflowX: "scroll",
  },
}))
