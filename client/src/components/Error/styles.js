import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  errorContainer: {
    width: "fit-content",
    margin: "auto",
    marginTop: "50px",
    height: "15vh",
  },
}));
