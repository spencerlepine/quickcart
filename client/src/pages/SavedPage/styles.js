import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  savedTitle: {
    width: 'fit-content',
    margin: '1em auto',
    padding: '0.5em',
    backgroundColor: 'orange',
    borderRadius: '1em',
  },
}));
