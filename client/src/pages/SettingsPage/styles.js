import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  settingsPage: {
    margin: 'auto',
    width: 'fit-content',
    background: 'white',
    padding: '1em',
  },
}));
