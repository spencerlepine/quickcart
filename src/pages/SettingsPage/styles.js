import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  settingsContainer: {
    width: 'fit-content',
    margin: '2em auto',
    padding: '10px 25px',
    background: 'white',
  },
}));