import { makeStyles } from "@material-ui/core/styles"

export default makeStyles(() => ({
  messageContainer: {
    width: "fit-content",
    margin: "2em auto",
    padding: "10px 25px",
    background: "white",
    minWidth: "100px",
    textAlign: "center",
    lineHeight: "1.5em",
  },
}))
