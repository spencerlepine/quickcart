import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
  logoLink: {
    '& img': {
      maxHeight: '45px',
      width: 'auto',
    },
    margin: 'auto',
  }
}))
