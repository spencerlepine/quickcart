import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  itemGrade: {
    color: 'white',
    textTransform: 'uppercase',
    padding: '1px 0.2em',
    fontWeight: '600',
    margin: '1px',
  }
}));