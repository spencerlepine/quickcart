import { makeStyles } from "@material-ui/core/styles"

export default makeStyles((theme) => ({
  itemDate: {
    gridColumnStart: 1,
    gridColumnEnd: 3,
    width: "fit-content",
    margin: "auto",
    padding: "1em",
  },
}))
